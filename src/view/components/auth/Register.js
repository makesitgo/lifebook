import React from 'react';
import PropTypes from 'prop-types';

const Register = () => {
  return (
    <div>
      Register
      <div>
        <p>sign up as a new user</p>
        <button onClick={() => console.log('TODO: implememnt user registration')}>register</button>
      </div>
    </div>
  )
}

Register.propTypes = {
};

export default Register;
